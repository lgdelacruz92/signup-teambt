import React from "react";
import * as MUI from "@material-ui/core";

const BrandingTitle = props => {
  return (
    <MUI.Typography align="center" variant="h1">
      BloomTrac
    </MUI.Typography>
  );
};

export default BrandingTitle;
